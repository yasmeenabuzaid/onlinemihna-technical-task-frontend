"use client";
import {
    Box, Typography
} from '@mui/material';

export default function TalentsPage() {
    return (
        <Box>
            <Typography variant="h4" fontWeight="bold" sx={{ mb: 3, color: '#1e293b' }}>
            </Typography>
        </Box>
    );
}